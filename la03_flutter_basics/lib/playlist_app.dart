import 'package:flutter/material.dart';

/// A music playlist browser app extracted into a StatelessWidget.
///
/// Move your MaterialApp widget tree from Exercise 1 into the [build] method
/// below, replacing the [Placeholder]. Then update `main.dart` to import this
/// file and use `const PlaylistApp()` in `runApp()`.
class PlaylistApp extends StatelessWidget {
  const PlaylistApp({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: Replace this Placeholder with your MaterialApp from Exercise 1.
    return const Placeholder();
  }
}
